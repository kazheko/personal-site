# Contracts Overview

No external HTTP or RPC contracts are required for this feature because header and footer partials operate entirely within Hugo templates. This placeholder documents that no API definitions are expected.
